<UserControl x:Class="FanaBridge.UI.ScreenSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:local="clr-namespace:FanaBridge.UI">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="8">

            <!-- Shown when the wheel has an ITM display -->
            <Border x:Name="borderItmInfo" Visibility="Collapsed"
                    Background="#1A4488CC" BorderBrush="#4488CC" BorderThickness="1"
                    CornerRadius="4" Padding="10,8" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Foreground="#AADDFF" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,8">
                        &#x2139;  This wheel has an ITM display. ITM mode shows a multi-page telemetry dashboard (speed, gear, lap info, fuel, car settings, lap times, tyre temps) instead of the basic display below.
                    </TextBlock>
                    <CheckBox x:Name="chkItmEnabled" Content="Enable ITM Display"
                              Checked="ChkItmEnabled_Changed" Unchecked="ChkItmEnabled_Changed"
                              Foreground="#AADDFF" />

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Page" Foreground="#AADDFF" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="cmbItmPage"
                                  SelectionChanged="CmbItmPage_SelectionChanged"
                                  Width="160">
                            <ComboBoxItem Content="Auto" Tag="0" />
                            <ComboBoxItem Content="1 - Lap Info" Tag="1" />
                            <ComboBoxItem Content="2 - Fuel / ERS" Tag="2" />
                            <ComboBoxItem Content="3 - Car Settings" Tag="3" />
                            <ComboBoxItem Content="4 - Lap Times" Tag="4" />
                            <ComboBoxItem Content="5 - Tyre Temps" Tag="5" />
                        </ComboBox>
                    </StackPanel>

                    <!-- Auto page settings — shown only when Auto is selected -->
                    <Border x:Name="borderAutoSettings" Visibility="Collapsed"
                            BorderBrush="#33AADDFF" BorderThickness="0,1,0,0"
                            Margin="0,12,0,0" Padding="0,8,0,0">
                        <local:ItmAutoSettingsPanel x:Name="autoSettingsPanel"/>
                    </Border>
                </StackPanel>
            </Border>

            <styles:SHSubSection Title="Display Mode">
                <ComboBox x:Name="cmbDisplayMode"
                          SelectionChanged="CmbDisplayMode_SelectionChanged"
                          Width="200" HorizontalAlignment="Left">
                    <ComboBoxItem Content="Gear" Tag="Gear" />
                    <ComboBoxItem Content="Speed" Tag="Speed" />
                    <ComboBoxItem Content="Gear + Speed" Tag="GearAndSpeed" />
                    <ComboBoxItem Content="Gear + Upshift Brackets" Tag="GearUpshiftBrackets" />
                </ComboBox>
            </styles:SHSubSection>

        </StackPanel>
    </ScrollViewer>
</UserControl>
